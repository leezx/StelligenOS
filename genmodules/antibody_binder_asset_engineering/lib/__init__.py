"""Computation library for antibody_binder_asset_engineering@0.3.1.

Every module here returns records that carry an explicit ``method`` and, where a
declared tool was used, its resolved version. A missing tool degrades to a
declared fallback and is reported as such; it is never silently substituted.
"""

LIB_VERSION = "0.2.0"

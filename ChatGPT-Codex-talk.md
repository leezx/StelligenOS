# ChatGPT-Codex 交互规范

本文是 StelligenOS 里给 ChatGPT/Codex 使用的固定交互规范。
以后只要没有新的覆盖性说明，就按本文执行。

## 1. 角色分工

- 我负责执行。
- 你负责下达目标、提供审核结论，或把我生成的审核指令转给其他模型。
- 我不替你做最终审批。
- 我不假装是外部审查者。

## 2. 工作模式

### 2.1 你直接给我任务时

我按“实现/整理/修订/验证”模式工作，直接完成你要求的内容。

### 2.2 你要求审核时

我不自己给最终审查结论，而是输出一份可直接转发给其他模型的审核指令。

### 2.3 你要求“只负责执行”时

我只做以下事情：

- 读取你指定的材料；
- 执行你指定的修改；
- 更新必要的 repo 文档；
- 运行验证；
- 输出结果和待确认项。

我不会主动扩展范围，不会提前设计下一阶段，也不会把审核职责拿过来自己判断。

## 3. 固定行为

### 3.1 读取优先

先读你指定的文件，再动手修改。

### 3.2 变更最小化

只改完成目标所需的最小范围。

### 3.3 证据留痕

任何 substantial task 结束后，必须同步更新：

- `logs/worklog.md`
- 必要时更新 `HANDOFF.md`
- 必要时更新相关 repo 文档、manifest、checklist、prompt

### 3.4 验证优先

修改后要做相应验证，例如：

- 边界脚本检查
- YAML 解析
- Markdown 链接检查
- 测试或 lint

如果验证失败，我会继续修到通过，或者明确说明阻塞原因。

## 4. 审核指令模板

当你需要把材料发给其他模型审核时，我输出下面这种格式，你可以原样转发：

```text
审核对象：StelligenOS
审核阶段：Phase X

请只审核本阶段范围内的变更，不要提前设计下一阶段。

请读取并检查：
1. PR 完整 diff
2. 所有 commits
3. Phase report
4. Review checklist
5. Manifest
6. 测试或验证结果
7. 与 main 分支当前架构规范的冲突

请按以下结论之一输出：
- APPROVE
- APPROVE_WITH_NONBLOCKING_COMMENTS
- REQUEST_CHANGES
- REJECT_PHASE

请重点检查：
- 是否越过当前 Phase 范围
- 是否引入了禁止的数据、缓存或临时产物
- 是否破坏仓库边界
- 是否与已冻结架构冲突
- 是否缺少必要的报告、清单、manifest 或验证

不要：
- 扩展到非必要重构
- 提前设计下一 Phase
- 仅给抽象建议而不落到具体文件或差异
```

## 5. 我输出审核指令时的默认原则

当你说“给我审核指令”时，我默认会：

- 指定审核对象；
- 指定 Phase；
- 列出必须看的文件；
- 列出可用的审批结论；
- 明确审核边界；
- 明确禁止项。

如果你没特别说，我会保持指令简短、可直接转发。

## 6. 我不做的事

- 不代替你做最终审批
- 不绕过你指定的审核流程
- 不把未要求的优化硬塞进任务
- 不在仓库里放大数据、原始数据、缓存、输出或临时工件
- 不把聊天内容当作正式规范来源

## 7. 推荐使用方式

你以后可以直接这样跟我说：

### 7.1 执行任务

```text
读取 <path>，按现有规范修改，并更新 worklog。
```

### 7.2 生成审核指令

```text
给我一份可以转发给其他大模型的审核指令，审核当前 Phase。
```

### 7.3 只负责执行

```text
我把审核交给别的模型，你只负责执行和留痕。
```

## 8. 当前约定

从此以后，默认按本文与 ChatGPT 交互：

- 你决定是否需要审核；
- 我只负责执行、整理、验证和留痕；
- 需要审核时，我输出可转发的审核指令；
- 最终审核结果由你或你指定的其他模型给出。

# ChatGPT-Codex 交互规范

本文件定义 StelligenOS 里 ChatGPT/Codex 的默认协作方式。
如果没有新的覆盖性说明，就按本文执行。

## 1. 总原则

- 我负责执行。
- 你负责决定是否需要审核，以及把我的材料转给其他模型或人审。
- 最终审核不由我自己完成。
- 默认以 PR 作为审核单元，而不是本地未推送工作区。
- GitHub 是交付和审计中间层；我不要求你在 ChatGPT 与 Codex 之间搬运文件。
- 你只负责拍板：合并、打回，或要求补充审核。

## 2. 强制 PR 流程

当任务需要审核时，默认流程是：

1. 我先从 `main` 拉取最新代码。
2. 我创建任务分支，命名为 `task_<编号>_<简短名>`。
3. 我在任务分支上修改，并先运行 `git status` 检查范围。
4. 我只用 `git add -- <相关文件>` 暂存确认过的文件，禁止 `git add .`、`git add -A` 和 `git add --all`。
5. 我提交语义清楚的 commit，格式为 `task_id: 简短说明`。
6. 我推送任务分支并创建指向 `main` 的 PR。
7. 我在 `docs/handoff/` 写入本次交接备忘，记录改动、未改动、验证和下一步。
8. 你把 PR 链接交给 ChatGPT 或其他模型审核；审核只看 PR 的 diff、commits、handoff、report、checklist、manifest 和验证结果。
9. 我只按当前 PR 的审核意见修订；通过后由你决定 merge 或打回。

标准命令如下，`<相关文件>` 必须替换成明确的文件路径：

```bash
git checkout main
git pull origin main
git checkout -b task_<编号>_<简短名>
# 修改代码或文档
git status --short
git add -- <相关文件>
git diff --cached --stat
git commit -m "task_id: 简短说明"
git push -u origin task_<编号>_<简短名>
```

如果使用仓库同步脚本，必须显式传入要提交的文件；脚本不得替代人工范围检查。
同步脚本会先打印工作区状态，并在暂存区非空时拒绝执行；它会直接暂存显式文件清单，因此未跟踪文件也能进入提交。脚本行为测试位于 `tests/test_git_sync.sh`。

## 3. 我什么时候直接执行

如果你直接给我修改目标，我就直接执行，不先停下来问流程。

我会做这些事：

- 读取你指定的材料；
- 修改最小必要文件；
- 更新 worklog 和必要的 handoff；
- 运行验证；
- 给出结果和待确认项。

如果任务进入 PR 交付，我还会创建或更新 `docs/handoff/` 下的交接备忘，并在回复中给出 PR 链接和可转发的审核指令。

## 4. 我什么时候输出审核指令

如果你说“给我审核指令”或“我要发给其他模型审核”，我会输出一份可直接转发的 PR 审核指令。

这份指令默认包含：

- 审核对象
- 审核阶段
- PR 范围
- 必看文件
- 允许的结论
- 明确的审核边界
- 明确的禁止项

## 5. 审核指令模板

你可以直接把下面这段发给其他模型：

```text
审核对象：StelligenOS
审核方式：PR 审核
审核阶段：Phase X

请只审核当前 PR，不要扩展到本次 PR 以外的未来计划或额外优化。
请把审核边界限制在当前 PR diff、相关 commits、report、checklist、manifest 和验证结果。

请读取并检查：
1. PR 完整 diff
2. 所有 commits
3. Phase report
4. Review checklist
5. Manifest
6. 测试或验证结果
7. 与 main 分支当前架构规范的冲突

请按以下结论之一输出：
- APPROVE
- APPROVE_WITH_NONBLOCKING_COMMENTS
- REQUEST_CHANGES
- REJECT_PHASE

请重点检查：
- 是否越过当前 Phase 范围
- 是否引入了禁止的数据、缓存或临时产物
- 是否破坏仓库边界
- 是否与已冻结架构冲突
- 是否缺少必要的报告、清单、manifest 或验证

不要：
- 扩展到非必要重构
- 提前设计下一 Phase
- 把未来优化算进本次审核范围
- 只给抽象建议而不落到具体文件或差异
```

## 6. Handoff 要求

每个任务 PR 都要有一个 `docs/handoff/` 交接备忘，至少包含：

- 任务编号、分支名、PR 链接和 commit；
- 本次改了什么，以及明确没改什么；
- 验证命令和结果；
- 未决问题、风险和下一步；
- 数据边界声明：本仓库没有新增数据、缓存或结果文件。

交接备忘是审查和下一轮接手的入口，不替代正式架构契约、Phase report 或 manifest。

## 7. PR 写法要求

默认我会按下面的节奏配合你：

- 每个 Phase 一个 PR
- 一个 PR 尽量只覆盖一个清晰目标
- commit 名称要能看出意图
- PR 描述要能独立说明范围、产物、验证和未决项
- 需要外部审核时，我优先提供 PR diff 语境，不依赖本地工作区状态

## 8. 我不做的事

- 不替你做最终审批
- 不把本地未推送状态当成最终审核材料
- 不擅自扩大修改范围
- 不把未来 Phase 的设计塞进当前 PR
- 不把聊天记录当作正式规范来源
- 不使用全量暂存命令把未经检查的文件带入 PR

## 9. 推荐用法

### 8.1 直接执行

```text
读取 <path>，按现有规范修改，并更新 worklog。
```

### 8.2 生成审核指令

```text
给我一份可以转发给其他大模型的审核指令，按 PR 审核当前 Phase。
```

### 8.3 切换到 PR 流程

```text
这次用 PR。你只负责执行、提交、推送、更新记录，然后给我审核指令。
```

## 10. 当前约定

从现在起，默认按本文与 ChatGPT 交互：

- 你决定是否需要审核；
- 我负责执行、整理、验证和留痕；
- 需要审核时，我按 PR 语境输出可转发的审核指令；
- 最终审核结果由你或你指定的其他模型给出。

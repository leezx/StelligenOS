version: "1.0"
name: stelligenos-core-objects
description: Architecture-level object type registry; contains no object records.
objects:
  - Opportunity
  - ClinicalHypothesis
  - TargetHypothesis
  - BinderCandidate
  - ADCConstruct
  - LeadSeries
  - DevelopmentCandidate
  - Asset
rules:
  identity:
    required:
      - object_type
      - object_id
      - schema_version
  persistence: external-only

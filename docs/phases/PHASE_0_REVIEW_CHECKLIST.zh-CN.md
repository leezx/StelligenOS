# Phase 0 Review Checklist

- Confirm the repository is explicitly defined as a biotechnology asset operating system implementation repository.
- Confirm no large datasets, raw data, intermediate files, caches, outputs, or temporary artifacts were added.
- Confirm small reference examples, toy examples, report templates, demo assets, and golden test cases are allowed only as non-data-bearing supporting material.
- Confirm the boundary wording appears in `README.md`, `AGENTS.md`, `LINKS.md`, `architecture.md`, the architecture contract, and the migration prompt.
- Confirm `scripts/verify_repository_boundary.sh` exists and passes.
- Confirm `Phase 0.5` legacy inventory and migration matrix are documented.
- Confirm the Phase 0 report recommends `PROCEED_TO_PHASE_1`.

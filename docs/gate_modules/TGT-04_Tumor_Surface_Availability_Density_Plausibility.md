# TGT-04 — Tumor Surface Availability / Density Plausibility · MOD-TGT04 Construction Drawing

- Runtime Migration **PR E11** (`task_20260831_runtime-migration-pr-e11`)
- Machine contract: `src/contracts/gate_modules/tgt04_tumor_surface_availability_density_plausibility.yaml`
- Status: **construction contract + drawing only.** No implementation ships in
  this PR. `MIGRATION_PENDING` remains.

## What this document is

The frozen 施工图 for **MOD-TGT04**, the single primary Evidence Production
Module of Gate **TGT-04** under `ADC_TARGET_GATESET@1.0` /
`INST-CRC-REFRACTORY-ADC-TARGET-v1`.

CURRENT_SYSTEM v5 §6.4: *逐 Gate 绘制 Evidence Production Module 施工图；审核
通过后 Module 才可开工.* PR E11 delivers the drawing, the machine contract, the
validation / parity tests and the 17-item acceptance checklist. The Module
itself (`gate_modules/tgt04_tumor_surface_availability_density_plausibility/`) is
**PR E12**, and may start only after this contract is APPROVED. PR E12 also bumps
the TGT-04 `gate_binding` `primary_module_version` from `0.0.0` to `1.0.0`; **PR
E11 does not touch it, the binding, the registry or any existing test.** The only
existing file PR E11 mutates is an append to `logs/worklog.md`.

**Kernel invariant.** The kernel defines the contract; the Module implements it.
One-way dependency, exactly like `extensions/` and MOD-TGT01 / MOD-TGT02 /
MOD-TGT03 / MOD-TGT05 / MOD-TGT08: a future `gate_modules/` package MAY import
kernel objects / Gate identity / contracts; `src/` MUST NEVER import
`gate_modules/`. The Module may not modify the Gate id / name / candidate
ownership, the `gate_question`, the Evidence Ladder, the evidence ceiling, or the
fatal / unknown / conflict / inference semantics; it may not reason across Gates,
lower a measurement requirement, or turn `UNKNOWN` into `PASS` / `HOLD` / `KILL`.
TGT-04-specific: the `gate_question` is the **density-plausibility** question.
`INDIRECT_STRONG` membranous-IHC / surfaceomics evidence is an observation-level
**localization** authority only — it never propagates to a Gate-level proposed
Strength, and a localization-only completed landscape is `INCONCLUSIVE / UNKNOWN`.

## Three headline conclusions (审核方原话, frozen verbatim)

> **1. Surface localization is not antigen density.** INDIRECT_STRONG
> localization evidence may establish that the antigen is on the membrane, but
> only quantitative DIRECT evidence can answer the TGT-04 density-plausibility
> question.

> **2. Quantitative values are evidence, not thresholds.** MOD-TGT04 may
> preserve measured antigen-density values and units, but it never derives a
> universal ADC-effective density cutoff; density plausibility must arrive as an
> auditable upstream qualification.

> **3. Reproducible quantitative `NEGLIGIBLE_OR_UNDETECTABLE` surface antigen may
> surface only `POTENTIAL_FATAL_PATTERN`.** Low-but-present antigen is not
> automatically negative or fatal, and the Module never decides fatality or ADC
> efficacy.

**Core chain.** qualified CRC surface context × measurement layer ×
malignant-cell attribution × audited surface-availability-search completion →
Gate-neutral empirical surface observations → frozen rung → a **single-tier
grading authority** (only a qualifying `DIRECT` quantitative antigen-density
observation grants a graded Direction; a localization-only completed landscape
stays `INCONCLUSIVE / UNKNOWN`) → aggregate density-plausibility Direction ×
Strength → optional reproducible-negligible-antigen `fatal_review` → human
review / GateSet policy. It never becomes: surface localization → assumed
density; a measured molecules-per-cell value → a universal ADC-effective range; a
low-but-present antigen → assumed inadequate / fatal; RNA expression → assumed
surface protein; a `NEGATIVE` density call → `KILL`.

## The 17-item acceptance checklist

| # | Item | What MOD-TGT04 must satisfy (PR E12) |
|---|------|-------------------------------------|
| 1 | **Gate identity & version** | `TGT-04` / `1.0`, name *Tumor Surface Availability / Density Plausibility*, `ADC_TARGET_GATESET@1.0`, `L04`, `INST-CRC-REFRACTORY-ADC-TARGET-v1`. Inherited, not owned. |
| 2 | **Primary Module identity & version** | `MOD-TGT04`; `module_implementation_version` `0.0.0` (declared, not built). PR E12 builds it and bumps to `1.0.0`; PR E11 does not touch the binding. |
| 3 | **Gate question** | Verbatim (parity-tested): *Is the target present on the cell surface of CRC malignant cells at a density plausibly adequate for ADC payload delivery?* It does **not** answer whether the target is on the surface at all (a separate, weaker LOCALIZATION tier), TGT-02 coverage, TGT-03 persistence, TGT-06 internalization, TGT-05 index, or ADC efficacy. |
| 4 | **Admissible evidence classes** | Derived parity (exact set equality, parity-tested): `evidence_required` ∪ the DIRECT / INDIRECT_STRONG / WEAK admissible classes. Not admissible: TGT-01 modality; TGT-02 coverage as a surface claim; TGT-03 persistence; TGT-05 liability; TGT-06 internalization; TGT-07 shedding; TGT-08 landscape; **any putative universal "clinically effective antigen-density range"**. |
| 5 | **Evidence Ladder & ceiling** | Verbatim PR D (parity-tested). **DIRECT** — a quantitative cell-surface antigen density measurement on CRC malignant cells or well-matched CRC models (ceiling: establishes surface-density plausibility for an ADC). **INDIRECT_STRONG** — validated membranous IHC with a membranous staining pattern on CRC malignant cells / cell-surface proteomics showing plasma-membrane enrichment on CRC malignant cells (ceiling: **supports surface LOCALIZATION; does NOT discharge the quantitative antigen-density requirement**). **WEAK** — topology / signal-peptide / GO-term prediction / non-CRC cell lines only / bulk or RNA-only as a surface proxy. Ceiling: *quantitative cell-surface antigen density on CRC malignant cells; surface localization alone does not reach it.* **DIRECT is NOT a closed assay whitelist** — a quantitative surface-density method + a factual `assay_method` type + a `measurement_validation_status` / basis + an explicitly qualified surface context; absolute-quantitation flow / QIFIKIT / mass cytometry / quantitative surfaceomics are examples, not the exhaustive set. **Quantitative values are evidence, not thresholds** — the EP MAY / SHOULD preserve `reported_density_value` / `reported_density_unit`; the Module never compares them to any cutoff or invented range. |
| 6 | **Direction interpretation** | **Two-tier evidence architecture, single-tier grading authority.** Only a qualifying `DIRECT` quantitative antigen-density observation, on a completed audited landscape, grants a graded Direction. **TGT-04 Strength rule:** `if qualifying DIRECT density evidence exists → overall Strength = DIRECT; else → INCONCLUSIVE / UNKNOWN` (zero evidence_refs). **No E10-style "highest qualifying rung = Strength".** **Rung context authority (PR E11 review r1):** the "or well-matched CRC models" permission is **DIRECT-only** — a `DIRECT` quantitative density observation may qualify on `CRC_MALIGNANT_CELLS` **or** `WELL_MATCHED_CRC_MODEL`; an `INDIRECT_STRONG` membranous-IHC / surface-proteomics observation may qualify **only** on `surface_context_class == CRC_MALIGNANT_CELLS` (+ `malignant_cell_attribution` + auditable basis), a well-matched model localization observation is a CONTEXTUAL reading, never an `INDIRECT_STRONG` rung. **Both** a qualifying `DIRECT` and a qualifying `INDIRECT_STRONG` observation carry ≥ 1 auditable local `surface_context_id` (audit-integrity identity, **not** a grading axis — a localization-only landscape with a valid indirect set is still `INCONCLUSIVE / UNKNOWN`). Separate typed upstream facts, each with its own auditable basis: `surface_context_class` ∈ {`CRC_MALIGNANT_CELLS`, `WELL_MATCHED_CRC_MODEL`, `NON_CRC_MODEL`, `UNRESOLVED`}; `context_adequacy_status` ∈ {`QUALIFIED`, `NOT_ESTABLISHED`}; `surface_localization_status` ∈ {`SURFACE_LOCALIZED`, `NOT_SURFACE_LOCALIZED`, `MIXED_OR_UNRESOLVED`, `NOT_ESTABLISHED`}; `measurement_validation_status` ∈ {`QUALIFIED`, `NOT_ESTABLISHED`} (*"does this measurement qualify for DIRECT"* — `QUALIFIED` is **not** a positive density conclusion); `density_plausibility_status` ∈ {`PLAUSIBLY_ADEQUATE`, `NOT_PLAUSIBLY_ADEQUATE`, `MIXED_OR_UNRESOLVED`, `NOT_ESTABLISHED`} (upstream-qualified interpretation, **never** computed from a number); `surface_antigen_level` ∈ {`QUANTITATIVELY_PRESENT`, `LOW_BUT_PRESENT`, `NEGLIGIBLE_OR_UNDETECTABLE`, `MIXED_OR_UNRESOLVED`, `NOT_ESTABLISHED`} (frozen separately for the fatal path); `reproducibility_status` ∈ {`QUALIFIED`, `NOT_ESTABLISHED`}. **Density direction mapping** (applied to a qualifying DIRECT observation, in order): `surface_antigen_level == NEGLIGIBLE_OR_UNDETECTABLE` → OPPOSES; else `density_plausibility_status == PLAUSIBLY_ADEQUATE` → SUPPORTS; `NOT_PLAUSIBLY_ADEQUATE` → OPPOSES; `MIXED_OR_UNRESOLVED` / `NOT_ESTABLISHED` → NONDIRECTIONAL / CONTEXTUAL. **`LOW_BUT_PRESENT` alone → NOT automatically NEGATIVE, NOT a fatal input.** `SURFACE_LOCALIZED` + qualifying IHC / surfaceomics → an INDIRECT_STRONG localization observation that never contributes a Gate-level Direction or Strength; `NOT_SURFACE_LOCALIZED` alone → not a NEGATIVE Gate Direction. **Legal Direction × Strength pairs (exactly 5):** `POSITIVE / DIRECT`, `NEGATIVE / DIRECT`, `CONFLICTING / DIRECT`, `INCONCLUSIVE / DIRECT`, `INCONCLUSIVE / UNKNOWN`. No `INDIRECT_STRONG` graded pair; no `INCONCLUSIVE / WEAK`. `INCONCLUSIVE / DIRECT` is legal when qualifying quantitative density measurements exist but `density_plausibility_status` is `MIXED_OR_UNRESOLVED` / `NOT_ESTABLISHED`. A single observation is never a Direction. |
| 7 | **Allowed / forbidden inference** | Verbatim PR D (parity-tested). Allowed: *the antigen is on the cell surface (from INDIRECT_STRONG)* / *adequate surface-density plausibility (from DIRECT only)*. Forbidden: surface localization implies adequate antigen density; density plausibility implies ADC efficacy; RNA expression implies surface protein. **`inference_guard` pinned verbatim** — *The EVGAP-01 surface-localization lock … contributes surface-localization evidence to TGT-04 but CANNOT alone discharge the antigen-density requirement.* |
| 8 | **Fatal conditions** | Verbatim PR D (parity-tested): *reproducible quantitative evidence of negligible or undetectable cell-surface antigen on CRC malignant cells — the absence of a targetable surface antigen. This is NOT a comparison against a putative universal "clinically effective antigen-density range".* A single quantitative negligible / undetectable observation is a `DIRECT`-class, `OPPOSES_DENSITY_PLAUSIBILITY` observation — **not yet** a `NEGATIVE / DIRECT` proposal and **not** a reproducible fatal pattern; a `LOW_BUT_PRESENT` antigen is **not a fatal input at all**. **Machine detection:** DIRECT-class quantitative density observations, **each on CRC malignant cells only** — NOT a well-matched CRC model and not any model proxy (the frozen PR D fatal sentence is *"on CRC malignant cells"*; the DIRECT ladder's model permission is for ordinary Direction grading only, it does not extend fatal authority — a model NEGLIGIBLE observation may drive an ordinary `DIRECT OPPOSES` Direction but is never a `fatal_review` contributor) — each in a qualified `CRC_MALIGNANT_CELLS` surface context with `QUALIFIED` context adequacy + auditable basis + explicit `malignant_cell_attribution` + basis, each `measurement_validation_status == QUALIFIED` + auditable basis + a non-empty factual `assay_method`, each `surface_antigen_level == NEGLIGIBLE_OR_UNDETECTABLE` + auditable basis, on a completed audited landscape, **and reproducibility via Route A or Route B**: *Route A* — one auditable study explicitly establishing reproducibility (`reproducibility_status == QUALIFIED` + auditable `reproducibility_basis`, source / human normalization explicitly supports a reproducible negligible / undetectable antigen); *Route B* — convergent `NEGLIGIBLE_OR_UNDETECTABLE` across **at least two** independent qualified CRC **malignant-cell** surface-context identities (each `surface_context_class == CRC_MALIGNANT_CELLS`; a well-matched model identity does not count toward the convergence) — a deterministic *sufficient* convergence pattern, **not** the word-meaning of "reproducible" and **not** `> 2`. Explicitly excluded: `LOW_BUT_PRESENT`; a bare `NOT_PLAUSIBLY_ADEQUATE`; localization-only / RNA / non-CRC evidence; any threshold comparison. Human-only: whether "negligible / undetectable" is justified vs the assay's lower detection limit, whether low-but-present is genuinely inadequate, whether the contexts are independent, whether it is an artifact, whether the pattern satisfies the GateSet fatal policy. Machine emits **at most** `fatal_review` `status = POTENTIAL_FATAL_PATTERN` — **never** `PUBLIC_FATAL_SIGNAL_ESTABLISHED` / a canonical fatal flag / `KILL` / `HOLD` / `Decision`. No numeric / molecules-per-cell / ABC / %-positive / H-score threshold; no invented range. |
| 9 | **Evidence source plan** | `dominant_evidence_regime` `PUBLIC_HYBRID`; current instantiation `PUBLIC_ONLY`; no provider connected in PR E11. Direct authority — a validated quantitative cell-surface antigen-density measurement (open set, see item 5) on annotated CRC malignant cells / a `QUALIFIED` well-matched CRC model. INDIRECT_STRONG — validated membranous IHC (membranous pattern) / cell-surface proteomics (plasma-membrane enrichment) on CRC malignant cells. WEAK — topology / GO prediction / non-CRC only / bulk-or-RNA proxy. **Mandatory search-completion axes (NOT evidence prerequisites, NOT grading axes)**: quantitative cell-surface antigen density search; validated membranous IHC search; cell-surface proteomics / plasma-membrane enrichment search; subcellular localization search (covers subcellular localization, topology / GO prediction, non-CRC surface evidence, RNA surface proxy). A component *searched / exhausted with zero qualifying records* still counts complete. `public_surface_search_complete` == `all(four component states)`; a contradiction is HARD in E12. PR E12 carries a module-local typed `SurfaceAvailabilityCompletion` (a run record, **not** a seventh core object) with `attempted` / `landscape_as_of` / `search_scope` / `sources_searched` / the umbrella + four per-component states / `unresolved_items` / `qualifying_direct_surface_context_ids` / `qualifying_indirect_surface_context_ids` (**both** a qualifying DIRECT quantitative observation and a qualifying INDIRECT_STRONG localization observation produce a local surface-context set here — audit-integrity reconciliation only, **not** a grading axis: only DIRECT grants density-question grading authority, and the indirect set never lifts the Strength above `UNKNOWN`) / `audit_observation_id`; completion ↔ `SEARCH_COMPLETION_AUDIT` snapshot parity (E6 / E8 / E10 gene) locks **both** qualifying context sets, a missing / drifted / dedup-lost snapshot or a drift in either set → HARD. No antigen-density / molecules-per-cell / ABC / %-positive / H-score threshold; no invented range; raw quantitative values MAY be preserved as factual measurement. |
| 10 | **Input contract** | `candidate_id` + the candidate's single canonical target identity (no second drift-prone target arg); `instantiation_id` + `context_id` `CTX-CRC-REFRACTORY-MCRC` + `context_version` `1` HARD-pinned to the fixed Instantiation — **a separate namespace from each observation's LOCAL `surface_context_id`**; the frozen TGT-04 refs and versions; `run_id` / `code_commit` / `landscape_as_of` / `evidence_regime = PUBLIC_ONLY`; the declared surface search scope; existing evidence refs. No implicit default context. E12 HARD-checks every `observation.context_key == run.context_key` and `SurfaceAvailabilityCompletion.search_scope == run.surface_search_scope` (E8 gene). |
| 11 | **EvidencePackage output contract** | Atomic, Gate-NEUTRAL, immutable-by-ID; canonical `SourceIndex` provenance; classification-driving fields in `study_context` (`observation_kind`, `molecular_layer`, `assay_method`, `measurement_validation_status` + basis, `crc_specific`, `surface_context_class` + basis, `context_adequacy_status` + basis, `malignant_cell_attribution` + basis, `surface_localization_status` + basis, `density_plausibility_status` + basis, `surface_antigen_level` + basis, `reproducibility_status` + basis, `surface_context_id` / `surface_context_ids` — a **local** evidence-context namespace, never the canonical Instantiation `context_id` — `context_key`, `landscape_as_of`); a `QUANTITATIVE_SURFACE_DENSITY` measurement block MAY / SHOULD carry the raw factual `reported_density_value` / `reported_density_unit` / `reported_density_summary` (never compared to a threshold); **kind / fact-specific `study_context`** — never promote a non-CRC / prediction / RNA-proxy / audit observation's source study to a CRC malignant-cell surface density measurement (E8 gene). A `SEARCH_COMPLETION_AUDIT` EP carries the completion snapshot (field names == the typed completion's) and **must survive `(source_id, claim)` dedup**. **May say** the empirical fact — *"source reported ~12000 target molecules per cell on CRC malignant cells by QIFIKIT"*, *"validated membranous IHC showed a membranous pattern"*, *"quantitative flow detected negligible cell-surface target antigen"* — a literal source claim (incl. a raw number and unit) is an admissible fact. **May not say** `passes TGT-04` / `TGT-04 POSITIVE` / `TGT-04 NEGATIVE` / `adequate antigen density established` / `above / below a clinically effective range` / `fatal` / `the ADC will work`, or any TGT-02 / TGT-03 / TGT-06 conclusion. **Exact canonical reuse** — `observation_id` parity; every classification-driving field present AND equal (incl. `measurement_validation_status`, `surface_localization_status`); for a `QUANTITATIVE_SURFACE_DENSITY` observation the raw `reported_density_value` / `reported_density_unit` / `reported_density_summary` are **factual exact-reuse parity fields when present** — a drifted reused raw value / unit / summary is a HARD identity integrity failure (empirical-identity parity, not classification authority; the value still never drives a Direction, a fatal signal or a threshold comparison); the reused EP's own `source_type` / `source_identifier` / `locator` still equal the resolved canonical `SourceIndex` (E8 / E10 / E11 gene). **Dedup** — the improved TGT-03 rule: same `source_id` + same `claim` but a **different** `surface_context_id` → **both survive**; a true duplicate only when `source_id` AND `claim` AND every classification-driving fact AND the local surface-context identity all match; a `SEARCH_COMPLETION_AUDIT` EP is never a dedup loser. |
| 12 | **Assessment proposal envelope contract** | A **non-canonical, module-local proposal envelope** — not a `CandidateGateAssessment`. Carries the canonical identity pins (`candidate_id`, `instantiation_id`, `context_id` = `CTX-CRC-REFRACTORY-MCRC`, `context_version`, `gateset_id` / version, `gate_id` / version) + `proposed_direction` + `proposed_strength` (exactly one of the five legal item-6 pairs; never a Decision / score), `evidence_refs` `[{evidence_id, role}]`, `aggregation_rationale`, `critical_unknowns`, `evidence_ceiling`, the machine acceptance record. **Omits** `assessment_id` / `assessment_version` / the `review` block / any TGT-02 / TGT-03 / TGT-06 conclusion / a fatal flag / any density threshold or range. The reproducible-negligible-antigen signal lives in a separate **module-local `fatal_review` record on the run result**: `required` / `status` (`POTENTIAL_FATAL_PATTERN`) / `evidence_ids` / `surface_context_ids` / `antigen_level_class` (all `NEGLIGIBLE_OR_UNDETECTABLE`) / `context_qualification_basis_refs` / `measurement_validation_basis_refs` / `reproducibility_basis_refs` (Route A basis; empty for a pure Route B convergence) / `landscape_as_of` / `surface_search_scope`. `required = true` iff DIRECT-class quantitative **CRC-malignant-cell-only** `NEGLIGIBLE_OR_UNDETECTABLE` observations (`surface_context_class == CRC_MALIGNANT_CELLS`; a well-matched CRC model observation may drive an ordinary Direction but is **never** a fatal contributor), each `QUALIFIED` context + basis + explicit `malignant_cell_attribution` + basis + `QUALIFIED` measurement validation + basis + non-empty `assay_method`, on a completed audited landscape, AND reproducibility via Route A or Route B (Route B convergence across ≥ 2 independent qualified CRC **malignant-cell** surface-context identities); a `LOW_BUT_PRESENT` observation, and a well-matched-model observation, never contribute. **A `fatal_review` is an actionable handoff only on an accepted run** (E6 / E8 / E10 gene). PR E11 only defines the field lists. |
| 13 | **Machine acceptance criteria** | Every EP validates; the canonical target identity matches every observation; every `observation.context_key == run.context_key` and `completion.search_scope == run.surface_search_scope` (HARD); every `source_id` resolves to a matching canonical `SourceIndex` and every `evidence_id` resolves; **exact canonical EP reuse parity** passes; the **completion ↔ `SEARCH_COMPLETION_AUDIT` snapshot parity** passes; `public_surface_search_complete == all(four component states)`; every evidence class is in the item-4 admissible list and none in `not_admissible`; **RNA / prediction / non-CRC never above `WEAK`; localization never above `INDIRECT_STRONG` and never grants density grading authority**; **DIRECT is not gated on a closed assay whitelist** — `assay_method` stays OPEN while `measurement_validation_status` ∈ {`QUALIFIED`, `NOT_ESTABLISHED`} is CLOSED, DIRECT requires `QUALIFIED` + a non-empty auditable `measurement_validation_basis` **and** a non-empty factual `assay_method`, `NOT_ESTABLISHED` never reaches DIRECT; a `DIRECT` rung requires an **explicitly qualified** surface context (`surface_context_class` ∈ {`CRC_MALIGNANT_CELLS`, `WELL_MATCHED_CRC_MODEL`} + `context_adequacy_status == QUALIFIED` + auditable bases; a bare `crc_specific` never reaches a rung); an `INDIRECT_STRONG` localization rung is **narrower** — `surface_context_class == CRC_MALIGNANT_CELLS` only (never `WELL_MATCHED_CRC_MODEL` / `NON_CRC_MODEL`) + `QUALIFIED` context adequacy + auditable bases + explicit `malignant_cell_attribution` + basis; every classification-driving qualified status carries an auditable basis (a missing / drifted basis is HARD); the **local surface-context namespace** (incl. `completion.qualifying_direct_surface_context_ids` / `completion.qualifying_indirect_surface_context_ids`) is never the canonical Instantiation `context_id` (collapsing them is HARD) and every qualifying DIRECT **or qualifying INDIRECT_STRONG** observation carries an auditable local `surface_context_id` (a qualifying observation with none is HARD); a reused `QUANTITATIVE_SURFACE_DENSITY` EP with a drifted raw `reported_density_value` / `reported_density_unit` / `reported_density_summary` is a HARD identity failure; the **Gate-level `proposed_strength` is `DIRECT` iff a qualifying DIRECT quantitative observation exists, else `UNKNOWN`** — a proposal with `proposed_strength == INDIRECT_STRONG` or `WEAK` is a HARD failure; `proposed_direction × proposed_strength` is exactly one of the five legal item-6 pairs — `INCONCLUSIVE / UNKNOWN` carries zero `evidence_refs`, a graded `INCONCLUSIVE / DIRECT` carries `CONTEXTUAL` refs; the Direction follows the frozen item-6 `density_direction_mapping`; a `LOW_BUT_PRESENT` observation is never scored `CONTRADICTING` on the antigen-level fact alone and never contributes to `fatal_review`; **no numeric decision** is taken on a `reported_density_value`; the **mandatory surface landscape is complete** (no early grade on one dataset / observation); if `fatal_review.required = true` its status is `POTENTIAL_FATAL_PATTERN`, every contributor is a DIRECT-class quantitative **CRC-malignant-cell-only** (`surface_context_class == CRC_MALIGNANT_CELLS`; no well-matched model / localization / RNA / non-CRC / low-but-present contributor) `NEGLIGIBLE_OR_UNDETECTABLE` observation, the Route A / B basis is auditable, and it is surfaced only on an accepted run; `fatal_review` is not a proposal-envelope field; dedup (a same source + same claim + different `surface_context_id` keeps both; an audit EP is never the dedup loser); **no numeric / ranking score anywhere; no antigen-density / molecules-per-cell / ABC / %-positive / H-score threshold; no invented range**; **no TGT-02 coverage substitution, no TGT-03 persistence conclusion, no TGT-06 internalization conclusion; no `PUBLIC_FATAL_SIGNAL_ESTABLISHED` / `KILL` / `HOLD` / `Decision`** anywhere. On failure → rejected with a machine reason. A **HARD identity / provenance / completion-consistency / classification-qualification integrity failure** rejects the **whole run** — never degraded to an accepted `UNKNOWN` (PR E2 / E8 / E10 gene); `UNKNOWN` from a genuinely incomplete public surface search, or a completed landscape with no qualifying DIRECT quantitative density, is *not* an integrity failure. |
| 14 | **Human acceptance & review surface** | The human sees the proposed Direction × Strength + rationale; each EP's claim / source / interpretation_boundary (per-context evidence, assay / layer, measurement validation, malignant-cell attribution basis, surface-context basis, surface-localization basis, density-plausibility basis, surface-antigen-level basis, reproducibility basis, any raw reported density value / unit), the rung, `critical_unknowns`; the `SurfaceAvailabilityCompletion` snapshot; the module-local `fatal_review` record; the machine acceptance record. Human-only: scientific correctness of the aggregated direction / strength; whether the `density_plausibility` qualification is reasonable for the payload / DAR / bystander context (no universal range); whether a `LOW_BUT_PRESENT` antigen genuinely supports inadequate density; whether the assay's calibration / lower detection limit justifies "negligible / undetectable"; whether a well-matched CRC model is genuinely representative; whether a surface context is a genuinely qualified CRC malignant-cell context and the attribution convincing; whether the contexts / models are genuinely independent; whether an apparent negligible signal is an assay / epitope-masking artifact; whether reported reproducibility is convincing; whether the reproducible-negligible-antigen pattern satisfies the GateSet fatal policy; final approval → the review surface (not the Module) constructs the canonical `CandidateGateAssessment`. |
| 15 | **Failure / unknown / conflict behavior** | Retrieval failure → a machine failure reason, not a partial envelope. Only a qualifying DIRECT quantitative observation grants a graded Direction / DIRECT; every other completed landscape → `INCONCLUSIVE / UNKNOWN`. **Localization-only completed landscape** — surface localization established (INDIRECT_STRONG) + landscape completed / exhausted + no qualifying DIRECT quantitative density → `INCONCLUSIVE / UNKNOWN`, zero `evidence_refs`, `critical_unknown` *"a quantitative cell-surface antigen density measurement on CRC malignant cells"* = `EXPERIMENT_REQUIRED` — **and this holds even when a non-empty valid `qualifying_indirect_surface_context_ids` set is reconciled into the completion** (the indirect set is audit-integrity only, it never lifts the Strength above `UNKNOWN`); the EPs still preserve localization facts (verbatim: *"Only localization or RNA evidence available → strength stays UNKNOWN on the density question; it is not upgraded"*). **RNA / prediction only** → `INCONCLUSIVE / UNKNOWN` (never a surface claim). **Incomplete surface search** → `INCONCLUSIVE / UNKNOWN` — no early grade on one dataset / observation. **`CONFLICTING / DIRECT`** only with genuinely incompatible qualifying quantitative claims (plausibly adequate vs not / negligible) and no auditable context-specific characterization; a qualified context / assay-calibration variation is not automatically `CONFLICTING` — if it cannot answer overall density plausibility it is a graded `INCONCLUSIVE / DIRECT`. **`LOW_BUT_PRESENT`** — SUPPORTING / CONTEXTUAL by default; OPPOSES only with an auditable `density_plausibility_status == NOT_PLAUSIBLY_ADEQUATE`; never a `fatal_review` contribution; never a "below a clinically effective range" statement. **`EXPERIMENT_REQUIRED`** allowed but narrow — only once the enumerated public surface source space is exhausted AND a new quantitative density measurement is needed; while any unresolved public item remains the resolution stays `PUBLIC_RESOLVABLE` / `CURRENTLY_UNRESOLVABLE` — do **not** auto-add `EXPERIMENT_REQUIRED` (E8 / E10 precedence). `UNKNOWN` is never silently converted to PASS / HOLD / KILL; `UNKNOWN` from an incomplete search or a completed localization-only landscape is not an integrity failure; surface localization is never upgraded to a density claim; a scientific `NEGATIVE` is never a fatal flag / `KILL`; RNA never becomes a surface claim; density plausibility never implies ADC efficacy. |
| 16 | **Stop rule** | The mandatory public surface-availability-search landscape is completed and audited BEFORE any graded Direction; a single quantitative dataset / localization dataset / negligible-antigen observation is never a completed answer; the stop is bounded. Normal stop — landscape complete + audited, the density Direction determined if a qualifying DIRECT quantitative observation exists (else `INCONCLUSIVE / UNKNOWN`), remaining public evidence cannot change the Direction or the DIRECT-vs-UNKNOWN Strength. Exhaustion stop — source space exhausted, residual `critical_unknown` classified. A reproducible negligible / undetectable pattern may stop the chase for weaker localization / prediction / RNA-proxy evidence, but **only after** the necessary surface-search completeness is satisfied; the Module never stops on the first negligible-antigen observation. |
| 17 | **Downstream consumer & handoff** | Hands off EvidencePackages + one proposal envelope to the human review surface, and — independently — the module-local `fatal_review` record for human Gate review + the GateSet fatal policy. On `HUMAN_APPROVED` the canonical `CandidateGateAssessment` feeds the MatrixView cell for `(candidate_id, TGT-04)` (PR C), the `ADC_TARGET_GATESET` decision layer (PR B), and TGT-06 **as context only** — TGT-04 never discharges TGT-06's own internalization measurement. Does not construct a `CandidateGateAssessment` / `HUMAN_APPROVED` record, does not produce a Decision / `KILL`, does not emit `PUBLIC_FATAL_SIGNAL_ESTABLISHED`, does not make a TGT-02 / TGT-03 / TGT-06 conclusion, does not let surface localization discharge the density requirement, does not derive a density threshold / range from a measured value, does not grade before the landscape is complete, does not write another Gate's index, does not modify the Matrix. |

## Normalized surface observation & the typed completion (conceptual shape, PR E12 freezes it)

A **normalized surface observation** (provider fact only — no rung, no direction,
no pass / fail):

`observation_id` · `target_identity` · `context_key` · `landscape_as_of` ·
`observation_kind` ∈ {`QUANTITATIVE_SURFACE_DENSITY`, `MEMBRANOUS_IHC`,
`SURFACE_PROTEOMICS`, `SUBCELLULAR_LOCALIZATION`, `TOPOLOGY_OR_GO_PREDICTION`,
`NON_CRC_SURFACE_EVIDENCE`, `RNA_SURFACE_PROXY`, `SEARCH_COMPLETION_AUDIT`} ·
`molecular_layer` ∈ {`PROTEIN`, `TRANSCRIPT`, `BOTH`, ``} · `assay_method` (a
factual type — **open set**, not a closed assay whitelist) ·
`measurement_validation_status` ∈ {`QUALIFIED`, `NOT_ESTABLISHED`} +
`measurement_validation_basis` (**closed predicate** — DIRECT requires
`QUALIFIED` + an auditable basis **and** a non-empty `assay_method`; `QUALIFIED`
is not a positive density conclusion) · `crc_specific` · `surface_context_class`
∈ {`CRC_MALIGNANT_CELLS`, `WELL_MATCHED_CRC_MODEL`, `NON_CRC_MODEL`,
`UNRESOLVED`} + `surface_context_basis` · `context_adequacy_status` ∈
{`QUALIFIED`, `NOT_ESTABLISHED`} + `context_adequacy_basis` ·
`malignant_cell_attribution` ∈ {`MALIGNANT`, `NON_MALIGNANT`, `UNRESOLVED`} +
`malignant_attribution_basis` · `surface_localization_status` ∈
{`SURFACE_LOCALIZED`, `NOT_SURFACE_LOCALIZED`, `MIXED_OR_UNRESOLVED`,
`NOT_ESTABLISHED`} + `surface_localization_basis` · `density_plausibility_status`
∈ {`PLAUSIBLY_ADEQUATE`, `NOT_PLAUSIBLY_ADEQUATE`, `MIXED_OR_UNRESOLVED`,
`NOT_ESTABLISHED`} + `density_plausibility_basis` ∈ {`SOURCE_REPORTED`,
`HUMAN_REVIEWED_NORMALIZATION`} (an upstream-qualified scientific interpretation —
**never** computed from a number) · `surface_antigen_level` ∈
{`QUANTITATIVELY_PRESENT`, `LOW_BUT_PRESENT`, `NEGLIGIBLE_OR_UNDETECTABLE`,
`MIXED_OR_UNRESOLVED`, `NOT_ESTABLISHED`} + `surface_antigen_level_basis` (a
factual class, frozen separately for the fatal path so `LOW_BUT_PRESENT` is never
silently equated with `NOT_PLAUSIBLY_ADEQUATE`) · `reproducibility_status` ∈
{`QUALIFIED`, `NOT_ESTABLISHED`} + `reproducibility_basis` · `surface_context_id`
/ `surface_context_ids` (a declared multi-context analysis; a **local**
evidence-context namespace, never the canonical Instantiation `context_id`
`CTX-CRC-REFRACTORY-MCRC`) · optional `reported_density_value` /
`reported_density_unit` / `reported_density_summary` (raw factual measurement,
never compared to a threshold) · `source_id` + source metadata + `retrieved_at`.

The typed **`SurfaceAvailabilityCompletion`** (a run-level machine record,
**not** a seventh core object): `attempted` · `landscape_as_of` · `search_scope`
· `sources_searched` · `public_surface_search_complete` (== `all` of the next
four) · `quantitative_surface_density_search_complete` ·
`membranous_ihc_search_complete` · `surface_proteomics_search_complete` ·
`subcellular_localization_search_complete` · `unresolved_items` ·
`qualifying_direct_surface_context_ids` · `qualifying_indirect_surface_context_ids`
· `audit_observation_id`. **Mandatory search = search-completion axes, not
evidence prerequisites and not grading axes** — a component *searched / exhausted
with zero qualifying records* still counts complete; it exists only so the Module
cannot find one pretty QIFIKIT dataset, stop searching the other axes, and
cherry-pick a Direction. **Both** a qualifying DIRECT quantitative density
observation and a qualifying INDIRECT_STRONG localization observation are
reconciled into a local surface-context set here
(`qualifying_direct_surface_context_ids` / `qualifying_indirect_surface_context_ids`
respectively) — **audit-integrity reconciliation only, not a grading axis**: only
DIRECT grants density-question grading authority, and the indirect set never
lifts the Strength above `UNKNOWN` (a localization-only landscape with a valid
indirect set is still `INCONCLUSIVE / UNKNOWN`). The `SEARCH_COMPLETION_AUDIT`
observation carries a structured snapshot of this record locking **both**
qualifying context sets; the snapshot fields are exact-reuse parity keys and a
drift / missing / dedup-lost snapshot, or a drift in either qualifying context
set, is a HARD run-level integrity failure in PR E12.

## What PR E11 is NOT

- No `gate_modules/tgt04_tumor_surface_availability_density_plausibility/` — that
  is PR E12.
- No provider / adapter / retrieval / runner / EvidencePackage or proposal
  runtime / fatal detector runtime.
- No numeric / ranking score, no antigen-density / molecules-per-cell / ABC /
  %-positive / H-score threshold, no invented "clinically effective range", no
  new scientific ladder semantics.
- No generic GateModule framework or abstract base class.
- No change to MOD-TGT01 / MOD-TGT02 / MOD-TGT03 / MOD-TGT05 / MOD-TGT08, the
  binding, the registry, or any existing test. `MOD-TGT04`
  `primary_module_version` stays `0.0.0`; `MIGRATION_PENDING` remains. The only
  existing file PR E11 mutates is an append to `logs/worklog.md`.

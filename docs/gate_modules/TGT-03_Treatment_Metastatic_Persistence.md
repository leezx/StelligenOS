# TGT-03 — Treatment / Metastatic Persistence · MOD-TGT03 Construction Drawing

- Runtime Migration **PR E9** (`task_20260830_runtime-migration-pr-e9`)
- Machine contract: `src/contracts/gate_modules/tgt03_treatment_metastatic_persistence.yaml`
- Status: **construction contract + drawing only.** No implementation ships in
  this PR. `MIGRATION_PENDING` remains.

## What this document is

The frozen 施工图 for **MOD-TGT03**, the single primary Evidence Production
Module of Gate **TGT-03** under `ADC_TARGET_GATESET@1.0` /
`INST-CRC-REFRACTORY-ADC-TARGET-v1`.

CURRENT_SYSTEM v5 §6.4: *逐 Gate 绘制 Evidence Production Module 施工图；审核
通过后 Module 才可开工.* PR E9 delivers the drawing, the machine contract, the
validation / parity tests and the 17-item acceptance checklist. The Module
itself (`gate_modules/tgt03_treatment_metastatic_persistence/`) is **PR E10**,
and may start only after this contract is APPROVED. PR E10 also bumps the TGT-03
`gate_binding` `primary_module_version` from `0.0.0` to `1.0.0`; **PR E9 does not
touch it, the binding, the registry or any existing test.** The only existing
file PR E9 mutates is an append to `logs/worklog.md`.

**Kernel invariant.** The kernel defines the contract; the Module implements it.
One-way dependency, exactly like `extensions/` and MOD-TGT01 / MOD-TGT02 /
MOD-TGT05 / MOD-TGT08: a future `gate_modules/` package MAY import kernel
objects / Gate identity / contracts; `src/` MUST NEVER import `gate_modules/`.
The Module may not modify the Gate id / name / candidate ownership, the
`gate_question`, the Evidence Ladder, the evidence ceiling, or the fatal /
unknown / conflict / inference semantics; it may not reason across Gates, lower a
measurement requirement, or turn `UNKNOWN` into `PASS` / `HOLD` / `KILL`.

## Three headline conclusions (审核方原话, frozen verbatim)

> **1. Baseline expression is not persistence.** TGT-02 evidence cannot discharge
> TGT-03 unless the evidence source itself explicitly measures a qualified
> treatment / metastasis context.

> **2. TGT-03 is a bidirectional scientific persistence gate.** Qualified
> clinical-context retention supports `POSITIVE`, qualified clinical-context near
> / marked loss supports `NEGATIVE`; `NEGATIVE` is not fatal and not `KILL`.

> **3. Reproducible protein-level near / marked loss may trigger only
> `POTENTIAL_FATAL_PATTERN`.** Reproducibility requires auditable evidence and is
> not defined solely by a numeric context count. The Module never decides
> fatality.

**Core chain.** qualified treatment / metastasis context × measurement layer ×
malignant-cell attribution × audited persistence-search completion → Gate-neutral
empirical persistence observations → frozen rung → aggregate persistence
Direction × Strength → optional reproducible-loss `fatal_review` → human review /
GateSet policy. It never becomes: baseline CRC expression → assumed persistence;
a minor down-regulation → assumed loss / fatal; two observations → automatically
"reproducible"; protein persistence → assumed surface density; `NEGATIVE` →
`KILL`.

## The 17-item acceptance checklist

| # | Item | What MOD-TGT03 must satisfy (PR E10) |
|---|------|-------------------------------------|
| 1 | **Gate identity & version** | `TGT-03` / `1.0`, name *Treatment / Metastatic Persistence*, `ADC_TARGET_GATESET@1.0`, `L04`, `INST-CRC-REFRACTORY-ADC-TARGET-v1`. Inherited, not owned. |
| 2 | **Primary Module identity & version** | `MOD-TGT03`; `module_implementation_version` `0.0.0` (declared, not built). PR E10 builds it and bumps to `1.0.0`; PR E9 does not touch the binding. |
| 3 | **Gate question** | Verbatim (parity-tested): *Does target expression persist in the actual clinical setting: refractory / prior-treated and/or metastatic CRC (including colorectal liver metastases)?* Not widened or narrowed. Does **not** answer TGT-02 baseline coverage, TGT-04 surface / density, or TGT-05 therapeutic index. |
| 4 | **Admissible evidence classes** | Derived parity (exact set equality, parity-tested): `evidence_required` ∪ the DIRECT / INDIRECT_STRONG / WEAK admissible classes. Not admissible: TGT-01 modality; treatment-naive baseline coverage *as a persistence claim* (admissible only as the frozen WEAK class); TGT-04/06 surface/density; TGT-05 liability; TGT-07 shedding; TGT-08 landscape. |
| 5 | **Evidence Ladder & ceiling** | Verbatim PR D (parity-tested). **DIRECT** — protein-level target retention in refractory / prior-treated CRC (malignant-cell attributed) / protein-level target expression in metastatic CRC lesions (malignant-cell attributed). **INDIRECT_STRONG** — treated / metastatic CRC malignant-compartment transcriptomics / a well-validated CRC treatment-resistance model (ceiling stays INDIRECT_STRONG even if it measures protein — the context is a model). **WEAK** — treatment-naive primary CRC only / different-tumor-type treated or metastatic. Ceiling: *protein-level target retention in refractory / prior-treated and/or metastatic CRC with malignant-cell attribution.* **DIRECT protein measurement is NOT a closed three-assay whitelist** — a protein-level clinical-context measurement + a factual `assay_method` type + a `protein_measurement_validation_status` / basis qualifies; validated IHC / quantitative proteomics / validated multiplex IF are examples, not the exhaustive set. |
| 6 | **Direction interpretation** | Bidirectional persistence gate. `POSITIVE` (retention supported) / `NEGATIVE` (clinically contextual target loss / materially impaired persistence) / `CONFLICTING` (genuinely incompatible overall persistence claims, no resolving characterization) / `INCONCLUSIVE`. `persistence_pattern` ∈ {`RETAINED`, `NEAR_LOSS_OR_MARKED_LOSS`, `TRANSIENT_OR_MINOR_DOWNREGULATION`, `MIXED_OR_UNRESOLVED`} is **upstream-qualified** (`persistence_pattern_basis` ∈ {`SOURCE_REPORTED`, `HUMAN_REVIEWED_NORMALIZATION`}), never computed from a fold-change / % / n; a missing / drifted basis is HARD. Mapping: `RETAINED` → `SUPPORTS_PERSISTENCE`; **`TRANSIENT_OR_MINOR_DOWNREGULATION` → `SUPPORTS_PERSISTENCE`** when the qualified observation establishes expression remains present in the relevant treated / metastatic context (it can contribute a POSITIVE Direction, **never** a NEGATIVE, **never** a `fatal_review` contribution, **never** TGT-04 density; only NONDIRECTIONAL / CONTEXTUAL when the observation is itself ambiguous about retention — "not sufficient for fatal" ≠ "scientifically non-directional"); `NEAR_LOSS_OR_MARKED_LOSS` → `OPPOSES_PERSISTENCE`; `MIXED_OR_UNRESOLVED` → NONDIRECTIONAL / CONTEXTUAL. Overall Strength = the **highest** qualifying frozen class (no E6-style two-axis rule; the four mandatory search components are search-space completeness, not a four-axis score). paired pre-/post protein is high-information DIRECT but **still just DIRECT** (no SUPER_DIRECT); a resistance model is INDIRECT_STRONG even if it measures protein. WEAK-only → `INCONCLUSIVE / UNKNOWN` (never `INCONCLUSIVE / WEAK`). A qualified context / site / time variation is **not** automatically `CONFLICTING` — if the source characterises it as context-dependent / variable, or it cannot answer overall persistence, that is a graded `INCONCLUSIVE`. A single observation is never a Direction. |
| 7 | **Allowed / forbidden inference** | Verbatim PR D (parity-tested). Allowed: *the target remains available in the actual refractory mCRC patient population.* Forbidden: baseline malignant-cell coverage (TGT-02) substitutes for demonstrated persistence; surface density (TGT-04) from persistence evidence. **`inference_guard` pinned verbatim** — *Generic EVGAP-02 CRC linkage does NOT support TGT-03 … An EVGAP-02 EvidencePackage contributes here only if its source and context explicitly qualify.* |
| 8 | **Fatal conditions** | Verbatim PR D (parity-tested): *reproducible protein-level near-loss or marked loss of the target under treatment or in metastatic CRC, resulting in loss of meaningful target availability in the intended refractory / metastatic context (transient or minor down-regulation is not sufficient).* A single clinical-context protein loss observation is a `DIRECT`-class, `NEGATIVE`-supporting observation — **not yet** a `NEGATIVE / DIRECT` proposal and **not** a reproducible fatal pattern; a transient / minor down-regulation is **not a fatal input at all**. **Machine detection criteria for a candidate pattern:** protein-level observations, each CRC malignant-cell attributed, each in an explicitly qualified treated / refractory / metastatic clinical persistence context with a `QUALIFIED` clinical-context adequacy status + auditable basis, each carrying `persistence_pattern == NEAR_LOSS_OR_MARKED_LOSS` (not `TRANSIENT_OR_MINOR_DOWNREGULATION`) + auditable `persistence_pattern_basis`, on a completed audited persistence landscape, **and reproducibility established by Route A or Route B**: *Route A* — one auditable study / analysis explicitly establishing reproducibility across paired / multiple patients / samples / contexts (`reproducibility_status == QUALIFIED` + auditable `reproducibility_basis`, source / human normalization explicitly supports reproducible marked / near loss); *Route B* — convergent `NEAR_LOSS_OR_MARKED_LOSS` across **at least two** independent qualified clinical context identities (a deterministic *sufficient* convergence pattern — **not** the word-meaning of "reproducible" and **not** `> 2`). Explicitly excluded from a fatal trigger: `TRANSIENT_OR_MINOR_DOWNREGULATION`, transcript-only, resistance-model-only, a different tumor type, treatment-naive primary CRC. Human-only: whether the loss is "marked" vs transient / minor, whether the contexts are independent, whether it is an assay / platform artifact, whether reported reproducibility is convincing, whether meaningful availability is actually lost, whether it satisfies the GateSet fatal policy. Machine emits **at most** a module-local `fatal_review` with `status = POTENTIAL_FATAL_PATTERN` — **never** `PUBLIC_FATAL_SIGNAL_ESTABLISHED` / a canonical fatal flag / `KILL` / `HOLD` / `Decision`. No numeric / %-positive / H-score / fold-change threshold. |
| 9 | **Evidence source plan** | `dominant_evidence_regime` `PUBLIC_HYBRID`; current instantiation `PUBLIC_ONLY`; no provider connected in PR E9. Direct authority — a validated protein-level clinical-context measurement (open set, see item 5) in annotated malignant cells in explicitly qualified refractory / prior-treated OR metastatic CRC. INDIRECT_STRONG — treated / metastatic malignant-compartment transcriptomics / a well-validated resistance model. WEAK — treatment-naive primary CRC only / different-tumor-type. **Mandatory search components (search-space completeness, NOT evidence prerequisites, NOT a four-axis score)**: refractory / prior-treated CRC expression; metastatic CRC lesion expression (covering liver / CRLM / lung / peritoneal — not one axis per organ); paired pre-/post-treatment CRC biopsy; validated CRC treatment-resistance model. A component *searched / exhausted with zero qualifying records* still counts as complete. `public_persistence_search_complete` == `all(four component states)`; a contradiction is HARD in E10. PR E10 carries a module-local typed `ClinicalPersistenceCompletion` (a run record, **not** a seventh core object) with `attempted` / `landscape_as_of` / `search_scope` / `sources_searched` / the umbrella + four per-component states / `unresolved_items` / `qualifying_direct_context_ids` / `qualifying_indirect_context_ids` / `audit_observation_id`; completion ↔ `SEARCH_COMPLETION_AUDIT` snapshot parity (E6 / E8 gene) is frozen for E10, a missing / drifted / dedup-lost snapshot → HARD. No fold-change / %-positive / H-score / down-regulation / context-count threshold. |
| 10 | **Input contract** | `candidate_id` + the candidate's single canonical target identity (no second drift-prone target arg); `instantiation_id` + `context_id` `CTX-CRC-REFRACTORY-MCRC` + `context_version` `1` HARD-pinned to the fixed Instantiation; the frozen TGT-03 refs and versions; `run_id` / `code_commit` / `landscape_as_of` / `evidence_regime = PUBLIC_ONLY`; the declared persistence search scope; existing evidence refs. No implicit default context. E10 HARD-checks every `observation.context_key == run.context_key` and `ClinicalPersistenceCompletion.search_scope == run.persistence_search_scope` (E8 gene). |
| 11 | **EvidencePackage output contract** | Atomic, Gate-NEUTRAL, immutable-by-ID; canonical `SourceIndex` provenance; classification-driving fields in `study_context` (`observation_kind`, `molecular_layer`, `assay_method`, `protein_measurement_validation_status`, `clinical_context` + basis, `context_adequacy_status` + basis, `malignant_cell_attribution` + basis, `persistence_pattern` + basis, `reproducibility_status` + basis, `context_id` / `context_ids`, `context_key`, `landscape_as_of`); **kind / fact-specific `study_context`** — never promote a non-refractory / non-metastatic / non-CRC / audit observation's source study to refractory mCRC (E8 gene). A `SEARCH_COMPLETION_AUDIT` EP carries the completion snapshot and **must survive `(source_id, claim)` dedup** — an attempted completion needs exactly one emitted / reused provenance-bearing matching audit EP (E8 gene). **May say** the empirical fact — *"source reported retained target protein staining in refractory CRC malignant cells"*, *"paired pre-/post biopsies showed a marked reduction of target protein"*, *"metastatic liver lesions retained target protein"* — a literal source persistence / loss claim is an admissible fact. **May not say** `passes TGT-03` / `TGT-03 POSITIVE` / `TGT-03 NEGATIVE` / `adequate persistence established` / `meaningful target availability is lost` / `fatal` / `should be killed`, or any TGT-02 substitution / TGT-04 conclusion; even when a Gate-relative phrase is the source's own claim the Module never upgrades it into a Gate conclusion. **Exact canonical reuse** — `observation_id` is part of the identity parity; every classification-driving field present AND equal; the reused EP's own `source_type` / `source_identifier` / `locator` still equal the resolved canonical `SourceIndex` (E8 gene); a missing / drifted field is HARD. |
| 12 | **Assessment proposal envelope contract** | A **non-canonical, module-local proposal envelope** — not a `CandidateGateAssessment`. Carries the canonical identity pins (`candidate_id`, `instantiation_id`, `context_id` = `CTX-CRC-REFRACTORY-MCRC`, `context_version`, `gateset_id` / version, `gate_id` / version) + `proposed_direction` + `proposed_strength` (`POSITIVE` / `NEGATIVE` / `CONFLICTING` / `INCONCLUSIVE` × `DIRECT` / `INDIRECT_STRONG` / `UNKNOWN`; never a Decision / score), `evidence_refs` `[{evidence_id, role}]`, `aggregation_rationale`, `critical_unknowns`, `evidence_ceiling`, the machine acceptance record. **Omits** `assessment_id` / `assessment_version` / the `review` block / any TGT-02 substitution / TGT-04 conclusion / a fatal flag. The reproducible-loss signal lives in a separate **module-local `fatal_review` record on the run result**: `required` / `status` (`POTENTIAL_FATAL_PATTERN`) / `evidence_ids` / `context_ids` / `persistence_class` (all `NEAR_LOSS_OR_MARKED_LOSS`) / `context_qualification_basis_refs` / `persistence_pattern_basis_refs` / **`reproducibility_basis_refs`** (Route A basis; empty for a pure Route B convergence) / `landscape_as_of` / `persistence_search_scope`. `required = true` iff protein-level CRC malignant-cell-attributed near / marked-loss observations, each in a qualified clinical context with `QUALIFIED` adequacy + basis + auditable `persistence_pattern_basis`, on a completed audited landscape, AND reproducibility via Route A or Route B. **A `fatal_review` is an actionable handoff only on an accepted run** (E6 / E8 gene). PR E9 only defines the field lists. |
| 13 | **Machine acceptance criteria** | Every EP validates; the canonical target identity matches every observation; every `observation.context_key == run.context_key` and `completion.search_scope == run.persistence_search_scope` (HARD); every `source_id` resolves to a matching canonical `SourceIndex` and every `evidence_id` resolves; **exact canonical EP reuse parity** passes (`observation_id` parity, classification-driving parity, reused-EP canonical-provenance re-check); the **completion ↔ `SEARCH_COMPLETION_AUDIT` snapshot parity** passes (exactly one normalized matching audit observation AND exactly one emitted / reused provenance-bearing matching audit EP; a missing / drifted / dedup-lost snapshot is HARD); `public_persistence_search_complete == all(four component states)`; every evidence class is in the item-4 admissible list and none in `not_admissible`; **transcript / resistance-model never above `INDIRECT_STRONG`**; treatment-naive primary CRC never above `WEAK` and never a persistence claim; a different tumor type never becomes CRC persistence; protein without malignant-cell attribution never `DIRECT`; **DIRECT is not gated on a closed three-assay whitelist**; every `QUALIFIED` clinical-context adequacy status carries an auditable `clinical_context_basis` + `context_adequacy_basis` and every `NEAR_LOSS_OR_MARKED_LOSS` / `TRANSIENT_OR_MINOR_DOWNREGULATION` pattern carries an auditable `persistence_pattern_basis` (a missing / drifted basis is HARD); a Route A qualification carries `reproducibility_status == QUALIFIED` + auditable `reproducibility_basis`, a Route B convergence is auditable as ≥ 2 independent qualified context identities; the **mandatory persistence landscape is complete** for the path taken (no early grade on one paired series / cohort / loss observation); `proposed_direction × proposed_strength` is exactly the item-6 truth-table result — **WEAK-only → `INCONCLUSIVE / UNKNOWN` (never `INCONCLUSIVE / WEAK`)**, `INCONCLUSIVE / UNKNOWN` carries zero `evidence_refs`, a graded `INCONCLUSIVE` carries `CONTEXTUAL` refs; a `TRANSIENT_OR_MINOR_DOWNREGULATION` observation is never `CONTRADICTING` and never contributes to `fatal_review`; if `fatal_review.required = true` its status is `POTENTIAL_FATAL_PATTERN`, every contributor is a protein-level clinical-context `NEAR_LOSS_OR_MARKED_LOSS` observation (no transcript / resistance-model / treatment-naive / different-tumor / transient-minor contributor), the Route A / B basis is auditable, and it is surfaced only on an accepted run; `fatal_review` is not a proposal-envelope field; dedup (incl. an audit EP is never the dedup loser); **no numeric / ranking score anywhere; no fold-change / %-positive / H-score / down-regulation / context-count threshold**; **no TGT-02 baseline-coverage substitution, no TGT-04 surface / density conclusion; no `PUBLIC_FATAL_SIGNAL_ESTABLISHED` / `KILL` / `HOLD` / `Decision`** anywhere. On failure → rejected with a machine reason. A **HARD identity / provenance / completion-consistency / classification-qualification integrity failure** rejects the **whole run** — never degraded to an accepted `UNKNOWN` (PR E2 / E8 gene); `UNKNOWN` from a genuinely incomplete public persistence search is *not* an integrity failure. |
| 14 | **Human acceptance & review surface** | The human sees the proposed Direction × Strength + rationale; each EP's claim / source / interpretation_boundary (per-context evidence, assay / layer, protein-measurement validation, malignant-cell attribution basis, clinical-context basis, persistence-pattern basis, reproducibility basis), the rung, `critical_unknowns`; the `ClinicalPersistenceCompletion` snapshot; the module-local `fatal_review` record; the machine acceptance record. Human-only: scientific correctness of the aggregated direction / strength; whether a context is a genuinely qualified refractory / prior-treated / metastatic context and the attribution convincing; whether the paired / pre-post relation is valid; whether the contexts are genuinely independent; whether the "marked / near loss" qualification is justified vs a transient / minor effect; whether an apparent loss is an assay / platform artifact; whether reported reproducibility is scientifically convincing; whether meaningful availability is actually lost; whether the reproducible-loss pattern satisfies the GateSet fatal policy; final approval → the review surface (not the Module) constructs the canonical `CandidateGateAssessment`. |
| 15 | **Failure / unknown / conflict behavior** | Retrieval failure → a machine failure reason, not a partial envelope. Direction / Strength follow the item-6 truth table. **Treatment-naive / different-tumor WEAK-only → `INCONCLUSIVE / UNKNOWN`** (verbatim *"Only treatment-naive primary CRC data -> UNKNOWN"*), zero `evidence_refs`. **Incomplete persistence search → `INCONCLUSIVE / UNKNOWN`** — no early grade on one paired series / cohort / loss observation. **`CONFLICTING`** only with genuinely incompatible overall persistence claims (retained vs materially impaired) and no auditable context-specific characterization; a qualified context / site / time variation is not automatically `CONFLICTING` — if it cannot answer overall persistence it is a graded `INCONCLUSIVE`. A qualified transient / minor down-regulation that still demonstrates retained expression is SUPPORTING (retention) — never `NEGATIVE`, never a `fatal_review` contribution, never TGT-04 density. **`EXPERIMENT_REQUIRED`** allowed but narrow — only once the enumerated public persistence source space is exhausted AND a new clinical-context measurement is needed (e.g. treated / metastatic transcript persistence exists but no qualified clinical-context protein confirmation → `POSITIVE / INDIRECT_STRONG` + `EXPERIMENT_REQUIRED`; only treatment-naive / different-tumor data + exhausted source space → `INCONCLUSIVE / UNKNOWN` + `EXPERIMENT_REQUIRED`); a paired pre-/post biopsy is one possible form, not the only requirement; while any unresolved public item remains the resolution stays `PUBLIC_RESOLVABLE` / `CURRENTLY_UNRESOLVABLE` — do **not** auto-add `EXPERIMENT_REQUIRED` (E8 precedence). `UNKNOWN` is never silently converted to PASS / HOLD / KILL; `UNKNOWN` from an incomplete public search is not an integrity failure; absence of persistence evidence is not a POSITIVE / retained call; a scientific `NEGATIVE` is never a fatal flag / `KILL`; baseline TGT-02 coverage never becomes a persistence claim. |
| 16 | **Stop rule** | The mandatory public persistence-search landscape is completed and audited BEFORE any graded Direction; a single paired biopsy series / metastatic cohort / loss observation is never a completed population-level answer; the stop is bounded. Normal stop — landscape complete + audited, highest qualifying class determined, remaining public evidence cannot change Direction / Strength. Exhaustion stop — source space exhausted, residual `critical_unknown` classified. A reproducible near / marked-loss pattern may stop the chase for weaker proxy evidence, but **only after** the necessary persistence-search completeness is satisfied; the Module never stops on the first loss observation. |
| 17 | **Downstream consumer & handoff** | Hands off EvidencePackages + one proposal envelope to the human review surface, and — independently — the module-local `fatal_review` record for human Gate review + the GateSet fatal policy. On `HUMAN_APPROVED` the canonical `CandidateGateAssessment` feeds the MatrixView cell for `(candidate_id, TGT-03)` (PR C), the `ADC_TARGET_GATESET` decision layer (PR B), and TGT-04 **as context only** — TGT-03 never discharges TGT-04's own surface / density measurement, and generic EVGAP-02 / CRC linkage never discharges TGT-03. Does not construct a `CandidateGateAssessment` / `HUMAN_APPROVED` record, does not produce a Decision / `KILL`, does not emit `PUBLIC_FATAL_SIGNAL_ESTABLISHED`, does not make a TGT-02 substitution or a TGT-04 conclusion, does not grade before the landscape is complete, does not write another Gate's index, does not modify the Matrix. |

## Normalized persistence observation & the typed completion (conceptual shape, PR E10 freezes it)

A **normalized persistence observation** (provider fact only — no rung, no
direction, no pass / fail):

`observation_id` · `target_identity` · `context_key` · `landscape_as_of` ·
`observation_kind` ∈ {`REFRACTORY_OR_PRIOR_TREATED_PROTEIN`,
`METASTATIC_LESION_PROTEIN`, `PAIRED_PRE_POST_PROTEIN`,
`TREATED_METASTATIC_TRANSCRIPT`, `RESISTANCE_MODEL`, `TREATMENT_NAIVE_PRIMARY`,
`DIFFERENT_TUMOR_TYPE`, `SEARCH_COMPLETION_AUDIT`} · `molecular_layer` ∈
{`PROTEIN`, `TRANSCRIPT`, `BOTH`, ``} · `assay_method` (a factual type — **open
set**, not a closed three-assay whitelist) · `protein_measurement_validation_status`
+ basis · `clinical_context` ∈ {`REFRACTORY_OR_PRIOR_TREATED`, `METASTATIC_CRC`,
`PAIRED_PRE_POST`, `RESISTANCE_MODEL`, `TREATMENT_NAIVE_PRIMARY`,
`DIFFERENT_TUMOR_TYPE`, ``} + `clinical_context_basis` · `context_adequacy_status`
∈ {`QUALIFIED`, `NOT_ESTABLISHED`} + `context_adequacy_basis` ·
`malignant_cell_attribution` ∈ {`MALIGNANT`, `NON_MALIGNANT`, `UNRESOLVED`} +
`malignant_attribution_basis` · `persistence_pattern` ∈ {`RETAINED`,
`NEAR_LOSS_OR_MARKED_LOSS`, `TRANSIENT_OR_MINOR_DOWNREGULATION`,
`MIXED_OR_UNRESOLVED`, ``} + `persistence_pattern_basis` ∈ {`SOURCE_REPORTED`,
`HUMAN_REVIEWED_NORMALIZATION`, ``} · `reproducibility_status` ∈ {`QUALIFIED`,
`NOT_ESTABLISHED`} + `reproducibility_basis` · `context_id` / `context_ids` (a
declared multi-context analysis) · `source_id` + source metadata +
`retrieved_at`.

The typed **`ClinicalPersistenceCompletion`** (a run-level machine record, **not**
a seventh core object): `attempted` · `landscape_as_of` · `search_scope` ·
`sources_searched` · `public_persistence_search_complete` (== `all` of the next
four) · `refractory_prior_treated_search_complete` ·
`metastatic_lesion_search_complete` · `paired_pre_post_search_complete` ·
`resistance_model_search_complete` · `unresolved_items` ·
`qualifying_direct_context_ids` · `qualifying_indirect_context_ids` ·
`audit_observation_id`. **Mandatory search = search-space completeness, not
evidence prerequisites** — a component *searched / exhausted with zero qualifying
records* still counts complete; it exists only so the Module cannot find one
pretty paired study, stop searching metastatic evidence, and cherry-pick a
Direction. The `SEARCH_COMPLETION_AUDIT` observation carries a structured snapshot
of this record; the snapshot fields are exact-reuse parity keys and a drift /
missing / dedup-lost snapshot is a HARD run-level integrity failure in PR E10.

## What PR E9 is NOT

- No `gate_modules/tgt03_treatment_metastatic_persistence/` — that is PR E10.
- No provider / adapter / retrieval / runner / EvidencePackage or proposal
  runtime / fatal detector runtime.
- No numeric / ranking score, no fold-change / %-positive / H-score /
  down-regulation / context-count threshold, no new scientific ladder semantics.
- No generic GateModule framework or abstract base class.
- No change to MOD-TGT01 / MOD-TGT02 / MOD-TGT05 / MOD-TGT08, the binding, the
  registry, or any existing test. `MOD-TGT03` `primary_module_version` stays
  `0.0.0`; `MIGRATION_PENDING` remains. The only existing file PR E9 mutates is an
  append to `logs/worklog.md`.
